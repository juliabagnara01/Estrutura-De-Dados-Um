#include <stdio.h>

int main() {
    int numero, contador;
    int soma = 0;

    for (contador = 1; contador <= 3; contador++) {
        printf("Digite um numero: ");
        scanf("%d", &numero);
        soma += numero;
    }
    printf("Soma total: %d\n", soma);
    return 0;
}
